#pragma once

/**
 * @file ads_base.h
 * @brief just algorythms
 */

#include <math.h>
#include <stddef.h>

/** @brief closest number what is pow of 2, which bigger then i */
size_t ads_fast_close_2(size_t i);
